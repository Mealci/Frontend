name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

permissions:
  contents: read
  pull-requests: write

jobs:
  ci-push:
    name: Vérifications CI (Base)
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Vérifier si la CI doit être exécutée
      id: check_run
      run: |
        if [[ ${{ github.event_name }} == 'push' ]]; then
          PR_URL=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                   "https://api.github.com/repos/${{ github.repository }}/pulls?head=${{ github.repository_owner }}:${{ github.ref_name }}&state=open" \
                   | jq -r '.[0].url')
          if [[ $PR_URL != "null" ]]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Une PR est active pour cette branche. Skipping CI pour le push."
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
        else
          echo "skip=false" >> $GITHUB_OUTPUT
        fi

    - name: Configuration de Flutter
      if: steps.check_run.outputs.skip != 'true'
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.2'
        channel: 'stable'
    
    - name: Désactiver les animations CLI
      if: steps.check_run.outputs.skip != 'true'
      run: flutter config --no-cli-animations
    
    - name: Obtenir les dépendances
      if: steps.check_run.outputs.skip != 'true'
      run: |
        echo -e "\e[1;34m🔍 Obtention des dépendances...\e[0m"
        flutter pub get
        echo -e "\e[1;32m✅ Dépendances récupérées avec succès !\e[0m"
    
    - name: Formater le code
      if: steps.check_run.outputs.skip != 'true'
      run: |
        echo -e "\e[1;34m🖌️ Formatage du code...\e[0m"
        dart format --set-exit-if-changed .
        echo -e "\e[1;32m✅ Code formaté avec succès !\e[0m"
    
    - name: Exécuter les tests
      if: steps.check_run.outputs.skip != 'true'
      run: |
        echo -e "\e[1;34m🧪 Exécution des tests...\e[0m"
        echo -e "Place holder pour flutter test"
        # flutter test
        echo -e "\e[1;32m✅ Tests terminés avec succès !\e[0m"
    
    - name: Vérifier la porte de qualité
      if: steps.check_run.outputs.skip != 'true' && success()
      run: |
        echo -e "\e[1;32m✅ Porte de qualité de base passée\e[0m"
    
    - name: Notifier l'échec
      if: steps.check_run.outputs.skip != 'true' && failure()
      run: |
        echo -e "\e[1;31m❌ Les vérifications de base ont échoué. Veuillez corriger les problèmes et mettre à jour.\e[0m"
        exit 1

  ci-pr:
    name: Vérifications CI (Pull Request)
    if: github.event_name == 'pull_request'
    needs: ci-push
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Configuration de Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.2'
        channel: 'stable'
    
    - name: Désactiver les animations CLI
      run: flutter config --no-cli-animations
    
    - name: Obtenir les dépendances
      run: flutter pub get
    
    - name: Analyser le code
      id: code_analysis
      run: |
        echo -e "\e[1;34m🔎 Analyse du code...\e[0m"
        echo "dart_output<<EOF" >> $GITHUB_OUTPUT
        dart analyze >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        echo "flutter_output<<EOF" >> $GITHUB_OUTPUT
        flutter analyze >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        echo -e "\e[1;32m✅ Analyse du code terminée !\e[0m"
    
    - name: Commenter les résultats de l'analyse
      uses: peter-evans/create-or-update-comment@v3
      with:
        issue-number: ${{ github.event.pull_request.number }}
        body: |
          ## Résultats de l'analyse de code
          
          ### Dart Analyze
          ```
          ${{ steps.code_analysis.outputs.dart_output }}
          ```
          
          ### Flutter Analyze
          ```
          ${{ steps.code_analysis.outputs.flutter_output }}
          ```
          
          Veuillez examiner ces résultats et apporter les améliorations nécessaires.

    - name: Exécuter les tests d'intégration
      run: |
        echo -e "\e[1;34m🧪 Exécution des tests d'intégration...\e[0m"
        echo "espace réservé pour flutter drive --target=test_driver/app.dart"
        echo -e "\e[1;32m✅ Tests d'intégration terminés avec succès !\e[0m"
    
    - name: Analyse de sécurité
      run: |
        echo -e "\e[1;34m🔒 Exécution de l'analyse de sécurité...\e[0m"
        echo "espace réservé pour l'outil d'analyse de sécurité"
        echo -e "\e[1;32m✅ Analyse de sécurité terminée !\e[0m"
    
    - name: Vérifier la porte de qualité PR
      if: success()
      run: |
        echo -e "\e[1;32m✅ Porte de qualité PR passée\e[0m"
    
    - name: Notifier l'échec PR
      if: failure()
      run: |
        echo -e "\e[1;31m❌ Les vérifications de PR ont échoué. Veuillez corriger les problèmes et mettre à jour la pull request.\e[0m"
        exit 1