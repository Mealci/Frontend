name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

permissions:
  contents: read
  pull-requests: write

jobs:
  ci-push:
    name: Vérifications CI (Base)
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Vérifier si la CI doit être exécutée
      id: check_run
      run: |
        if [[ ${{ github.event_name }} == 'push' ]]; then
          PR_URL=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                   "https://api.github.com/repos/${{ github.repository }}/pulls?head=${{ github.repository_owner }}:${{ github.ref_name }}&state=open" \
                   | jq -r '.[0].url')
          if [[ $PR_URL != "null" ]]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Une PR est active pour cette branche. Skipping CI pour le push."
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
        else
          echo "skip=false" >> $GITHUB_OUTPUT
        fi

    - name: Configuration de Flutter
      if: steps.check_run.outputs.skip != 'true'
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.2'
        channel: 'stable'
    
    - name: Désactiver les animations CLI
      if: steps.check_run.outputs.skip != 'true'
      run: flutter config --no-cli-animations
    
    - name: Obtenir les dépendances
      if: steps.check_run.outputs.skip != 'true'
      run: |
        echo -e "\e[1;34m🔍 Obtention des dépendances...\e[0m"
        flutter pub get
        echo -e "\e[1;32m✅ Dépendances récupérées avec succès !\e[0m"
    
    - name: Formater le code
      if: steps.check_run.outputs.skip != 'true'
      run: |
        echo -e "\e[1;34m🖌️ Formatage du code...\e[0m"
        dart format --set-exit-if-changed .
        echo -e "\e[1;32m✅ Code formaté avec succès !\e[0m"
    
    - name: Exécuter les tests
      if: steps.check_run.outputs.skip != 'true'
      run: |
        echo -e "\e[1;34m🧪 Exécution des tests...\e[0m"
        echo -e "Place holder pour flutter test"
        # flutter test
        echo -e "\e[1;32m✅ Tests terminés avec succès !\e[0m"
    
    - name: Vérifier la porte de qualité
      if: steps.check_run.outputs.skip != 'true' && success()
      run: |
        echo -e "\e[1;32m✅ Porte de qualité de base passée\e[0m"
    
    - name: Notifier l'échec
      if: steps.check_run.outputs.skip != 'true' && failure()
      run: |
        echo -e "\e[1;31m❌ Les vérifications de base ont échoué. Veuillez corriger les problèmes et mettre à jour.\e[0m"
        exit 1

  ci-pr:
    name: Vérifications CI (Pull Request)
    if: github.event_name == 'pull_request'
    needs: ci-push
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Configuration de Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.2'
        channel: 'stable'
    
    - name: Désactiver les animations CLI
      run: flutter config --no-cli-animations
    
    - name: Obtenir les dépendances
      run: |
        echo -e "\e[1;34m🔍 Obtention des dépendances...\e[0m"
        flutter pub add --dev dependency_validator
        flutter pub get
        echo -e "\e[1;32m✅ Dépendances récupérées avec succès !\e[0m"
    
    - name: Analyser le code
      id: code_analysis
      run: |
        echo -e "\e[1;34m🔎 Analyse du code...\e[0m"
        dart analyze > dart_analysis.txt
        flutter analyze > flutter_analysis.txt
        echo -e "\e[1;32m✅ Analyse du code terminée !\e[0m"

    - name: Exécuter les tests d'intégration
      run: |
        echo -e "\e[1;34m🧪 Exécution des tests d'intégration...\e[0m"
        echo "espace réservé pour flutter drive --target=test_driver/app.dart"
        echo -e "\e[1;32m✅ Tests d'intégration terminés avec succès !\e[0m"
    
    - name: Vérification des vulnérabilités
      id: vulnerability_check
      run: |
        echo -e "\e[1;34m🛡️ Vérification des vulnérabilités connues...\e[0m"
        vulnerability_output=""
        while read pkg; do
          result=$(curl -s "https://pub.dev/api/packages/${pkg%@*}/versions/${pkg#*@}" | jq -r '.vulnerability')
          if [ "$result" != "null" ]; then
            vulnerability_output+="⚠️ Vulnérabilité détectée pour $pkg: $result\n"
          fi
        done < <(flutter pub deps --json | jq -r '.packages[] | .name + "@" + .version')
        echo "vulnerability_output<<EOF" >> $GITHUB_ENV
        echo "$vulnerability_output" >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV
        echo -e "\e[1;32m✅ Vérification des vulnérabilités terminée !\e[0m"
    
    - name: Vérifier la porte de qualité PR
      if: success()
      run: |
        echo -e "\e[1;32m✅ Porte de qualité PR passée\e[0m"
    
    - name: Notifier l'échec PR
      if: failure()
      run: |
        echo -e "\e[1;31m❌ Les vérifications de PR ont échoué. Veuillez corriger les problèmes et mettre à jour la pull request.\e[0m"
        exit 1

    - name: Préparer le commentaire
      id: prepare_comment
      run: |
        COMMENT_BODY=""
        
        # Fonction pour vérifier si l'analyse contient des problèmes
        has_issues() {
          ! grep -qE "^No issues found" <<< "$1"
        }
        
        # Vérifier l'analyse du code Dart
        DART_OUTPUT=$(cat dart_analysis.txt)
        if has_issues "$DART_OUTPUT"; then
          COMMENT_BODY+="### Analyse du code Dart\n\`\`\`\n$DART_OUTPUT\n\`\`\`\n\n"
        fi
        
        # Vérifier l'analyse du code Flutter
        FLUTTER_OUTPUT=$(cat flutter_analysis.txt)
        if has_issues "$FLUTTER_OUTPUT"; then
          COMMENT_BODY+="### Analyse du code Flutter\n\`\`\`\n$FLUTTER_OUTPUT\n\`\`\`\n\n"
        fi
        
        # Vérifier les vulnérabilités
        if [[ -n "$VULNERABILITY_OUTPUT" ]]; then
          COMMENT_BODY+="### Analyse de sécurité\n\`\`\`\n$VULNERABILITY_OUTPUT\n\`\`\`\n\n"
        fi
        
        # Si aucune information n'a été ajoutée, indiquer que tout est propre
        if [[ -z "$COMMENT_BODY" ]]; then
          COMMENT_BODY="✅ Toutes les vérifications sont passées. Le code est propre et prêt à être fusionné."
        else
          COMMENT_BODY="## Résultats des vérifications CI\n\n${COMMENT_BODY}\nVeuillez examiner ces résultats et apporter les améliorations nécessaires."
        fi
        
        echo "comment_body<<EOF" >> $GITHUB_OUTPUT
        echo "$COMMENT_BODY" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT

    - name: Commenter les résultats de l'analyse
      if: always()
      uses: peter-evans/create-or-update-comment@v3
      with:
        issue-number: ${{ github.event.pull_request.number }}
        body: ${{ steps.prepare_comment.outputs.comment_body }}